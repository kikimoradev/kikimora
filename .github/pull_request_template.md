# What and why

# Checklist

- [ ] `pnpm check` passes locally
- [ ] New code is covered by tests (`vitest.config.ts` enforces coverage thresholds)
- [ ] Docs updated when behavior or configuration changed (README, docs/, CLAUDE.md)
